import React, { Component } from 'react';

class Hoteld extends Component {
  render() {
    c;
    return (
      <div>
        <p>{hotels.map(r => r.name)}</p>
      </div>
    );
  }
}

export default Hoteld;
